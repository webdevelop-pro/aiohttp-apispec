# aiohttp-apispec 3.1.0 artifacts

These distributions were built from source commit
`f4ef1408cc4c17bcab6014655caaf5db3774c186`, tagged `v3.1.0`, with
`python -m build`. Both artifacts passed `python -m twine check`.

The SHA-256 digests in `SHA256SUMS` are the release integrity contract.
